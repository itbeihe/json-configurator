var ObjectField = JsonC.inherit(BaseField,{
    constructor:ObjectField,
    // 获取值 isNative是否返回原始值
    getVal:function(isNative){
    },
    // 重新注册ns到cellmap中
    reSignNS:function(ns){

    },
    // 验证方法
    validate:function(){

    },
    // 初始化方法
    init:function(conf){
        this.super(conf);
    }
});
